name: fuzzy_time
description: "A simple, dependency-free Flutter/Dart package that converts DateTime and Duration objects into human-friendly conversational fuzzy strings."
version: 0.0.1
homepage: https://inteist.com/
repository: https://github.com/inteist/fuzzy_time

environment:
  sdk: ^3.10.4

dependencies:

dev_dependencies:
  lints: ^6.1.0
  test: ^1.24.0
